export function StarterPage() {
  return (
    <main className="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8">
      <p className="text-sm tracking-[0.16em] text-muted-foreground uppercase">
        React application starter
      </p>
      <h1 className="mt-3 max-w-2xl text-4xl font-semibold tracking-tight sm:text-5xl">
        Foundation ready for feature development
      </h1>
      <p className="mt-5 max-w-2xl text-base leading-7 text-muted-foreground">
        Routing, shared UI, styling, testing, linting, and production builds are
        configured. Business features will be introduced one milestone at a time.
      </p>
    </main>
  )
}
